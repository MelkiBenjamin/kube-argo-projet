apiVersion: v1
kind: Service
metadata:
  name: {{ include "projet.fullname" . }}
  labels:
    {{- include "projet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "projet.selectorLabels" . | nindent 4 }}
